export type ActiveView = 'generate' | 'mcp';

export interface FormState {
  tracePath: string;
  vmidPath: string;
  machineProfilePath: string;
  programName: string;
  referencePath: string;
  outputPath: string;
  postId: string;
}
